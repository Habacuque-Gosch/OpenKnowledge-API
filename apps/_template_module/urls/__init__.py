# ──────────────────────────────────────────────
# DO NOT add paths here. Split into sub-modules:
#   urls_<domain>.py
# ──────────────────────────────────────────────

# from .urls_<domain> import urlpatterns as <domain>_urls

app_name = '<module_name>'

urlpatterns = [
    # *<domain>_urls,
]
